# Jarvis dashboard — frontend env (Phase 4 sub-phase 4.0).
# Copy to .env.local and fill in. .env.local is gitignored; this file is not.

# Auth.js v5 session secret. Used to sign/encrypt the dashboard session cookie.
# Independent of the backend in the BFF model (the browser never talks to the
# backend directly). Generate: openssl rand -hex 32
AUTH_SECRET=

# The single-master passkey checked by the Credentials provider (src/auth.ts).
# This is what you type on the login screen.
MASTER_PASSKEY=

# --- server-side only (never NEXT_PUBLIC_*; the browser never sees these) ---
# The FastAPI backend base URL. Host dev: http://localhost:8000.
# Containerised later: http://backend:8000 (compose service DNS).
JARVIS_BACKEND_URL=http://localhost:8000
# The backend's API_SECRET_KEY. BFF route handlers (src/app/api/**) attach this
# as X-API-Key and proxy to the backend after verifying the dashboard session.
JARVIS_API_KEY=

# --- voice-in (Phase 4.2 wake-word) ---
# Picovoice access key for the on-device "Jarvis" wake-word (Porcupine, WASM).
# Free at https://console.picovoice.ai . NEXT_PUBLIC_* = available in the browser
# (the wake-word runs client-side). Empty = wake-word toggle is inert (text/voice
# still work). The command STT after wake uses the browser Web Speech API (Chrome).
NEXT_PUBLIC_PICOVOICE_ACCESS_KEY=
